[model]
name = Index
label = {{ this.title }}
protected = yes
hidden = yes

[fields.title]
label = Title
type = string

[fields.body]
label = Body
type = markdown

[children]
model = post
order_by = -pub_date, title
